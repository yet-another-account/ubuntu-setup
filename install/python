#!/bin/bash

[ -e ~/.install/python ] && exit

echo "######### Installing python #########"

sudo apt-get install -y software-properties-common build-essential python-pip python3-pip || exit 1

pip install -U pip || exit 1
pip3 install -U pip || exit 1

pip install virtualenv || exit 1
pip3 install virtualenv || exit 1

# needed for matplotlib
apt-get install libpng-dev libfreetype6-dev -y || exit 1

mkdir -p ~/.install && touch ~/.install/python
